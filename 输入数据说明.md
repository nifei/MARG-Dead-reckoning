# capture/*.csv 文件格式说明

## 概述

`capture/` 目录下的 CSV 文件包含从 MARG/IMU 传感器（MPU-9250）采集的原始数据，用于行人航位推算（PDR）和零速更新（ZUPT）分析。

## 文件命名规范

文件名格式：`{放置位置}_{性别}_{运动路径}.csv`

- **放置位置**：
  - `handheld` - 手持
  - `foot` - 脚部
- **性别**：
  - `male` - 男性
  - `female` - 女性
- **运动路径**：
  - `circ` - 圆形路径
  - `rect` - 矩形路径
  - `trig` - 三角形路径

## 数据格式

### 表头结构

文件采用**多级表头**结构，共 3 行表头：

| 行号 | 说明 |
|------|------|
| 第 1 行 | 传感器类型（accel, gyro, mag, Q） |
| 第 2 行 | 坐标轴（x, y, z, w） |
| 第 3 行 | 索引列标识（time） |

### 列结构

| 列索引 | 传感器类型 | 坐标轴 | 单位 | 说明 |
|--------|------------|--------|------|------|
| 0 | - | - | 毫秒 | 时间戳 |
| 1 | accel | x | m/s² | 加速度计 X 轴 |
| 2 | accel | y | m/s² | 加速度计 Y 轴 |
| 3 | accel | z | m/s² | 加速度计 Z 轴 |
| 4 | gyro | x | rad/s | 陀螺仪 X 轴 |
| 5 | gyro | y | rad/s | 陀螺仪 Y 轴 |
| 6 | gyro | z | rad/s | 陀螺仪 Z 轴 |
| 7 | mag | x | μT | 磁力计 X 轴 |
| 8 | mag | y | μT | 磁力计 Y 轴 |
| 9 | mag | z | μT | 磁力计 Z 轴 |
| 10 | Q | w | - | 四元数 w 分量 |
| 11 | Q | x | - | 四元数 x 分量 |
| 12 | Q | y | - | 四元数 y 分量 |
| 13 | Q | z | - | 四元数 z 分量 |

### 采样率

- **默认采样率**：200 Hz
- **时间间隔**：5 ms

## 数据示例

```csv
,accel,accel,accel,gyro,gyro,gyro,mag,mag,mag,Q,Q,Q,Q
,x,y,z,x,y,z,x,y,z,w,x,y,z
time,,,,,,,,,,,,,
1336378,-1.019946,5.78448,-8.370256,-0.0108778,0.0479235,-0.0218858,70.1137,92.0895,-111.1258,0.300274,0.949431,0.088391,-0.024511
1336409,-0.972061,5.736595,-8.375046,-0.0130078,0.0670985,-0.0304088,67.3920,92.9245,-113.2207,0.297001,0.950537,0.088142,-0.022324
```

## 坐标系说明

### NED 坐标系（北东地）

- **X 轴**：指向北方（North）
- **Y 轴**：指向东方（East）
- **Z 轴**：指向地面（Down）

### 传感器坐标系

传感器数据按照 NED  convention 进行处理。

## 数据预处理

在使用数据时，通常需要进行以下预处理：

1. **陀螺仪单位转换**：从弧度转换为角度
   ```python
   df['gyro'] *= 180 / np.pi
   ```

2. **低通滤波**：去除高频噪声
   ```python
   from scipy import signal
   b, a = signal.butter(10, 20, fs=200, btype='lowpass')
   df = df.apply(lambda x: signal.filtfilt(b, a, x))
   ```

## 可用数据集

capture/ 目录包含以下数据集：

| 文件名 | 描述 |
|--------|------|
| handheld_female_circ.csv | 女性手持圆形路径 |
| handheld_female_rect.csv | 女性手持矩形路径 |
| handheld_female_trig.csv | 女性手持三角形路径 |
| handheld_male_circ.csv | 男性手持圆形路径 |
| handheld_male_rect.csv | 男性手持矩形路径 |
| handheld_male_trig.csv | 男性手持三角形路径 |
| foot_female_circ.csv | 女性脚部圆形路径 |
| foot_female_rect.csv | 女性脚部矩形路径 |
| foot_female_trig.csv | 女性脚部三角形路径 |
| foot_male_circ.csv | 男性脚部圆形路径 |
| foot_male_rect.csv | 男性脚部矩形路径 |
| foot_male_trig.csv | 男性脚部三角形路径 |

## 注意事项

1. **校准**：原始数据未应用 `calib.csv` 中的校准参数，如需校准请参考 `PDR/pdr.py` 中的实现
2. **批量数据**：文件中可能包含重复的时间戳数据（批量发送）
3. **四元数**：Q 列提供了预计算的姿态四元数，也可以通过 AHRS 算法重新计算
